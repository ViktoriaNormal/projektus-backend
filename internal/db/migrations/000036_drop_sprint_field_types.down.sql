-- Откат 000036.
-- Миграция вверх удаляла данные из project_params и board_fields (с каскадом
-- на task_field_values). Восстановить удалённые строки без бэкапа нельзя,
-- поэтому down-шаг намеренно пустой: он лишь позволяет прогнать
-- `migrate down` без ошибки. Если нужно «вернуть» поля типа sprint/sprint_list
-- на конкретной инсталляции — восстанавливайте из дампа БД.
SELECT 1;
