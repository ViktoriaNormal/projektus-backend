-- Откат 000037. Миграция вверх обнуляла is_required для уже существовавших
-- кастомных полей/параметров; обратная операция безопасно не выполнима — мы
-- не знаем, какие именно строки были обязательными до миграции. Оставляем
-- no-op, чтобы `migrate down` не падал.
SELECT 1;
