-- =============================================================================
-- 000007: Добавить blocked_until в users для временной блокировки при неудачных попытках входа
-- =============================================================================

ALTER TABLE users ADD COLUMN blocked_until TIMESTAMP;
