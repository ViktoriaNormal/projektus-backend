openapi: 3.0.3
info:
  title: Projektus API
  description: |-
    Backend API for the Projektus project management system.

    ### PATCH-семантика для nullable-полей

    В PATCH-запросах для каждого nullable-поля (помеченного `nullable: true`) поддерживаются три состояния:

    | Тело запроса | Поведение |
    |---|---|
    | `{}` (поле отсутствует) | Значение не меняется |
    | `{ "field": null }` | Значение сбрасывается (записывается NULL в БД) |
    | `{ "field": "value" }` | Устанавливается новое значение |
  version: 2.0.0

servers:
  - url: /api/v1

security:
  - BearerAuth: []

tags:
  - name: Auth
    description: Authentication and password management
  - name: Users
    description: User profiles
  - name: Permissions
    description: System permission catalog
  - name: Notifications
    description: Notification feed and settings
  - name: Meetings
    description: Meeting management
  - name: Projects
    description: Project management
  - name: ProjectMembers
    description: Project membership
  - name: ProjectRoles
    description: Project-level role definitions
  - name: ProjectParams
    description: Project custom parameters
  - name: Boards
    description: Board management
  - name: Columns
    description: Board columns
  - name: Swimlanes
    description: Board swimlanes
  - name: Notes
    description: Column/swimlane notes
  - name: CustomFields
    description: Board custom fields
  - name: Tasks
    description: Task management
  - name: Sprints
    description: Sprint management
  - name: Backlog
    description: Product and sprint backlog
  - name: Tags
    description: Task tags
  - name: AdminRoles
    description: System role administration
  - name: AdminUsers
    description: User administration
  - name: AdminPasswordPolicy
    description: Password policy administration
  - name: AdminTemplates
    description: Project template administration
  - name: ScrumAnalytics
    description: Scrum analytics (velocity, burndown)
  - name: KanbanAnalytics
    description: Kanban analytics (CFD, cycle time, throughput, WIP)

paths:
  # ─── Auth ───────────────────────────────────────────────────────────
  /auth/register:
    post:
      tags: [Auth]
      summary: Register a new user (закрытая регистрация)
      description: |
        Публичная регистрация по умолчанию отключена. Эндпоинт возвращает
        `403 REGISTRATION_DISABLED` во всех окружениях, кроме тех, где явно
        установлено `ALLOW_PUBLIC_REGISTRATION=true` (только для отладочных
        целей). Учётные записи пользователей создаются администратором через
        `POST /admin/users`.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '200':
          description: Registered successfully (только при ALLOW_PUBLIC_REGISTRATION=true)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_AuthResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '403':
          description: Самостоятельная регистрация отключена (REGISTRATION_DISABLED)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/login:
    post:
      tags: [Auth]
      summary: Login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_AuthResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh access token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
      responses:
        '200':
          description: Tokens refreshed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_AuthResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Logout (invalidate refresh token)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LogoutRequest'
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /auth/password-policy:
    get:
      tags: [Auth]
      summary: Get password policy (public)
      security: []
      responses:
        '200':
          description: Password policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_PasswordPolicy'

  /auth/change-password:
    post:
      tags: [Auth]
      summary: Change current user password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ─── Permissions ────────────────────────────────────────────────────
  /permissions:
    get:
      tags: [Permissions]
      summary: List all permission definitions
      description: Returns permission definitions with code, scope, name, and description.
      responses:
        '200':
          description: Flat array of permission definitions
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/PermissionDefinitionResponse'

  # ─── Users ──────────────────────────────────────────────────────────
  /users:
    get:
      tags: [Users]
      summary: Search users
      description: |
        Возвращает страницу пользователей по фильтру `q` и полное число
        совпадений под фильтр в поле `total` (не зависит от limit/offset).
        Фронт использует `total` для счётчиков «Всего сотрудников: N» /
        «Найдено: M» и пагинатора без дополнительных запросов.

        Отдельный случай — `limit=0`: сервер вернёт только `total` с пустым
        массивом `users[]`. Полезно, когда нужно узнать количество совпадений
        без выгрузки самих данных.

        **Порядок сортировки** (применяется после фильтра q, до limit/offset):
        `ORDER BY LOWER(full_name) ASC, id ASC`. Регистро-независимо, кириллица
        сортируется по-русски корректно; `id` — стабильный tiebreaker для
        записей с одинаковым ФИО, чтобы пагинация не скакала.
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: limit
          in: query
          description: |
            Размер страницы. При `0` возвращает только `total` без users[].
            Отрицательное значение или отсутствие параметра трактуется как 20.
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: User list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [users, total]
                        properties:
                          users:
                            type: array
                            items:
                              $ref: '#/components/schemas/UserResponse'
                          total:
                            type: integer
                            format: int64
                            description: |
                              Полное количество пользователей, подходящих под фильтр `q`,
                              без учёта limit/offset.

  /users/{id}:
    get:
      tags: [Users]
      summary: Get user profile
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      responses:
        '200':
          description: User profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_User'
    patch:
      tags: [Users]
      summary: Update user profile
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserProfileRequest'
      responses:
        '200':
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_User'

  /users/{id}/avatar:
    put:
      tags: [Users]
      summary: Upload avatar
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Updated profile with avatar
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_User'

  /users/{id}/roles:
    get:
      tags: [Users]
      summary: Get user system roles
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      responses:
        '200':
          description: System roles
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RoleResponse'

  /users/{id}/project-roles:
    get:
      tags: [Users]
      summary: Get user project roles
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      responses:
        '200':
          description: Project roles
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectRoleResponse'

  # ─── Notifications ──────────────────────────────────────────────────
  /notifications:
    get:
      tags: [Notifications]
      summary: Get notification feed
      parameters:
        - name: unread_only
          in: query
          schema:
            type: boolean
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Notification feed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/NotificationFeedResponse'

  /notifications/read-all:
    post:
      tags: [Notifications]
      summary: Mark all notifications as read
      responses:
        '204':
          description: All marked as read

  /notifications/delete-all:
    post:
      tags: [Notifications]
      summary: Delete all notifications
      description: Permanently deletes all notifications for the current user.
      responses:
        '204':
          description: All notifications deleted

  /notifications/{notification_id}/read:
    post:
      tags: [Notifications]
      summary: Mark single notification as read
      parameters:
        - name: notification_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Marked as read

  /notifications/settings:
    get:
      tags: [Notifications]
      summary: Get notification settings
      description: |
        Возвращает настройки уведомлений пользователя для всех известных
        типов событий. При создании учётной записи (через `/admin/users`
        или при первичной инициализации первого администратора) настройки
        создаются автоматически со значениями `in_system=true`,
        `in_email=false` — список всегда непустой.
      responses:
        '200':
          description: Notification settings
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NotificationSettingResponse'
    put:
      tags: [Notifications]
      summary: Update notification settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/UpdateNotificationSettingItem'
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NotificationSettingResponse'

  # ─── Meetings ───────────────────────────────────────────────────────
  /meetings:
    get:
      tags: [Meetings]
      summary: List user meetings
      description: |
        Возвращает встречи, в которых текущий пользователь — организатор
        или принявший приглашение участник (`participants[].status = accepted`).
        Правило действует для всех пользователей, включая администраторов
        системы: системные права на просмотр чужих встреч не распространяются.
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Meeting list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/MeetingResponse'
    post:
      tags: [Meetings]
      summary: Create meeting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMeetingRequest'
      responses:
        '200':
          description: Created meeting with participants
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_MeetingDetails'

  /meetings/{meeting_id}:
    get:
      tags: [Meetings]
      summary: Get meeting details
      parameters:
        - $ref: '#/components/parameters/MeetingIdPath'
      responses:
        '200':
          description: Meeting details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_MeetingDetails'
    patch:
      tags: [Meetings]
      summary: Update meeting
      parameters:
        - $ref: '#/components/parameters/MeetingIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMeetingRequest'
      responses:
        '200':
          description: Updated meeting
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_MeetingDetails'

  /meetings/{meeting_id}/cancel:
    post:
      tags: [Meetings]
      summary: Cancel meeting
      parameters:
        - $ref: '#/components/parameters/MeetingIdPath'
      responses:
        '200':
          description: Cancelled meeting
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Meeting'

  /meetings/{meeting_id}/participants:
    get:
      tags: [Meetings]
      summary: List meeting participants
      parameters:
        - $ref: '#/components/parameters/MeetingIdPath'
      responses:
        '200':
          description: Participants
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          participants:
                            type: array
                            items:
                              $ref: '#/components/schemas/MeetingParticipantResponse'
    post:
      tags: [Meetings]
      summary: Add participants
      parameters:
        - $ref: '#/components/parameters/MeetingIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_ids]
              properties:
                user_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        '200':
          description: Added participants
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          participants:
                            type: array
                            items:
                              $ref: '#/components/schemas/MeetingParticipantResponse'

  /meetings/{meeting_id}/response:
    post:
      tags: [Meetings]
      summary: Respond to meeting invitation
      parameters:
        - $ref: '#/components/parameters/MeetingIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [accepted, declined]
      responses:
        '204':
          description: Response recorded

  # ─── Projects ───────────────────────────────────────────────────────
  /projects/references:
    get:
      tags: [Projects]
      summary: Get project references (enums, field types, etc.)
      responses:
        '200':
          description: References
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectReferencesResponse'

  /projects:
    get:
      tags: [Projects]
      summary: List user projects
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [active, archived]
        - name: project_type
          in: query
          schema:
            type: string
            enum: [scrum, kanban]
      responses:
        '200':
          description: Project list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectResponse'
    post:
      tags: [Projects]
      summary: Create project
      description: |
        Creates a new project. The structure (boards, columns, swimlanes,
        fields, project params, roles) is fully copied from the corresponding
        template (scrum/kanban), including `is_default`, `is_system` flags,
        descriptions, options, and the admin role with its permissions.

        The creator is automatically added as the first member with the
        admin role from the template.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectRequest'
      responses:
        '200':
          description: Created project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Project'

  /projects/{project_id}:
    get:
      tags: [Projects]
      summary: Get project details
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Project details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Project'
    patch:
      tags: [Projects]
      summary: Update project
      description: |-
        Каскад при смене `owner_id` (только если новое значение отличается от текущего):

        * Новый ответственный гарантированно становится участником проекта (строка в `project_members`) и получает admin-роль этого проекта (роль с `is_admin = true`).
        * Существующие роли нового участника (например «Разработчик», «Менеджер») сохраняются — admin-роль добавляется поверх, а не заменяет их.
        * Прежний ответственный сохраняет свои роли, включая admin-роль, — смена ответственного не лишает его прав на проекте.
        * Если `owner_id` не меняется, каскад не выполняется.
        * Если в проекте отсутствует admin-роль (нарушен инвариант БД) — возвращается `500 PROJECT_ADMIN_ROLE_MISSING`, транзакция откатывается, `owner_id` остаётся прежним.

        Вся операция (обновление полей проекта + каскад по участникам) выполняется в одной транзакции.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRequest'
      responses:
        '200':
          description: Updated project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Project'
        '500':
          description: В проекте отсутствует admin-роль (нарушен инвариант)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    delete:
      tags: [Projects]
      summary: Delete project
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - name: confirm
          in: query
          required: true
          schema:
            type: boolean
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  # ─── Project Members ────────────────────────────────────────────────
  /projects/{project_id}/members:
    get:
      tags: [ProjectMembers]
      summary: List project members
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Member list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectMemberResponse'
    post:
      tags: [ProjectMembers]
      summary: Add member
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddMemberRequest'
      responses:
        '200':
          description: Added member
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectMemberResponse'

  /projects/{project_id}/members/{member_id}:
    delete:
      tags: [ProjectMembers]
      summary: Remove member
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/MemberIdPath'
      responses:
        '200':
          description: Removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'
    patch:
      tags: [ProjectMembers]
      summary: Update member roles
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/MemberIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMemberRolesRequest'
      responses:
        '200':
          description: Roles updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  # ─── Project Permissions ────────────────────────────────────────────
  /projects/{project_id}/my-permissions:
    get:
      tags: [ProjectRoles]
      summary: Get my effective permissions for a project
      description: |-
        Returns the effective permissions of the current user for the given project.

        Logic:
        - If the user has system permission `system.projects.manage` with access `full`, all 7 areas return `full`.
        - If `view`, all 7 areas return `view`.
        - Otherwise, returns permissions from the user's project role.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Effective project permissions
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectPermission'

  # ─── Project Roles ──────────────────────────────────────────────────
  /projects/{project_id}/roles:
    get:
      tags: [ProjectRoles]
      summary: List project roles
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Role list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectRoleDefinitionResponse'
    post:
      tags: [ProjectRoles]
      summary: Create project role
      description: |
        Каждый `area` в `permissions[]` валидируется против каталога
        `catalog.AllPermissions` (scope=project). Неизвестные / чужого scope
        коды → `400 INVALID_PERMISSION_CODE` с перечнем значений в message.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectRoleRequest'
      responses:
        '200':
          description: Created role
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectRoleDefinitionResponse'

  /projects/{project_id}/roles/{role_id}:
    patch:
      tags: [ProjectRoles]
      summary: Update project role
      description: |
        Updates name, description, or permissions of a project role.

        Permissions are updated partially (upsert): only the areas included
        in the request are changed, other areas keep their current access level.

        **Admin role (`is_admin: true`) restrictions:**
        - `name` and `description` CAN be changed.
        - `permissions` CANNOT be changed — returns `400 PROJECT_ADMIN_ROLE`
          with message "Нельзя изменять права доступа роли администратора проекта".

        Каждый `area` в `permissions[]` валидируется против каталога
        `catalog.AllPermissions` (scope=project). Неизвестные / чужого scope
        коды → `400 INVALID_PERMISSION_CODE` с перечнем значений в message.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/RoleIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRoleRequest'
      responses:
        '200':
          description: Updated role
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectRoleDefinitionResponse'
    delete:
      tags: [ProjectRoles]
      summary: Delete project role
      description: |
        Deletes a project role. The admin role (`is_admin: true`) cannot be
        deleted — returns `400 PROJECT_ADMIN_ROLE`. A role that still has
        members assigned cannot be deleted — returns `400 ROLE_HAS_MEMBERS`.
        At least one member must retain the admin role (`LAST_PROJECT_ADMIN`).
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/RoleIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Project Params ─────────────────────────────────────────────────
  /projects/{project_id}/params:
    get:
      tags: [ProjectParams]
      summary: List project params
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Param list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectParamResponse'
    post:
      tags: [ProjectParams]
      summary: Create project param
      description: |-
        Если передано `value`, оно валидируется по `field_type` (см. правила в описании схемы `CreateProjectParamRequest`).
        Создание с `is_required: true` без `value` допустимо — пользователь заполнит позже.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectParamRequest'
      responses:
        '200':
          description: Created param
        '400':
          description: Validation error (value does not match field_type)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectParamResponse'

  /projects/{project_id}/params/{param_id}:
    patch:
      tags: [ProjectParams]
      summary: Update project param
      description: |-
        Обновляет параметр проекта. Поддерживается три состояния для `value`:
        - поле отсутствует → значение не меняется
        - `"value": null` → значение сбрасывается (NULL)
        - `"value": "..."` → устанавливается новое значение

        **Валидация:**
        - Если параметр обязательный (`is_required = true`), `value` не может быть пустым или null.
        - Если `is_required` устанавливается в `true`, а текущее `value` пустое — ошибка 400.
        - Новое `value` валидируется по `field_type` параметра (см. правила в описании схемы).
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/ParamIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectParamRequest'
      responses:
        '200':
          description: Updated param
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectParamResponse'
        '400':
          description: |-
            Validation error. Possible reasons:
            - Required param cannot have empty value
            - Value does not match field_type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [ProjectParams]
      summary: Delete project param
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/ParamIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Sprints (under projects) ───────────────────────────────────────
  /projects/{project_id}/sprints:
    get:
      tags: [Sprints]
      summary: List project sprints
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Sprint list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/SprintResponse'
    post:
      tags: [Sprints]
      summary: Create sprint
      description: |-
        Создаёт новый спринт со статусом planned.
        Серверная валидация: даты нового спринта не должны пересекаться с существующими незавершёнными спринтами (planned/active). При пересечении — 400.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSprintRequest'
      responses:
        '200':
          description: Created sprint
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SprintResponse'
        '400':
          description: Даты спринта пересекаются с существующим незавершённым спринтом

  # ─── Backlog ────────────────────────────────────────────────────────
  /projects/{project_id}/backlog/product:
    get:
      tags: [Backlog]
      summary: Get product backlog
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Task list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TaskResponse'

  /projects/{project_id}/backlog/product/tasks:
    post:
      tags: [Backlog]
      summary: Add task to product backlog
      description: |-
        Добавляет задачу в product backlog проекта.
        Если задача находится в каком-либо спринте — автоматически удаляет из sprint_tasks и сбрасывает column_id в NULL.
        Задача гарантированно будет только в одном месте: либо в product backlog, либо в спринте.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [task_id]
              properties:
                task_id:
                  type: string
                  format: uuid
                order:
                  type: integer
      responses:
        '200':
          description: Task added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /projects/{project_id}/backlog/product/tasks/{task_id}:
    delete:
      tags: [Backlog]
      summary: Remove task from product backlog
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Task removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /projects/{project_id}/backlog/product/reorder:
    patch:
      tags: [Backlog]
      summary: Reorder product backlog
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orders]
              properties:
                orders:
                  type: array
                  items:
                    type: object
                    required: [task_id, order]
                    properties:
                      task_id:
                        type: string
                        format: uuid
                      order:
                        type: integer
      responses:
        '200':
          description: Reordered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /projects/{project_id}/backlog/sprint:
    get:
      tags: [Backlog]
      summary: Get active sprint backlog
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      responses:
        '200':
          description: Task list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TaskResponse'

  /projects/{project_id}/backlog/move-to-sprint:
    post:
      tags: [Backlog]
      summary: Move tasks to sprint
      description: |-
        Перемещает задачи в указанный спринт.
        Если задача находится в другом спринте — автоматически удаляет из него перед добавлением.
        Если задача в product backlog — удаляет из бэклога.
        Задача гарантированно будет только в одном спринте.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sprint_id, task_ids]
              properties:
                sprint_id:
                  type: string
                  format: uuid
                task_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        '200':
          description: Tasks moved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  # ─── Scrum Analytics ────────────────────────────────────────────────
  /projects/{project_id}/analytics/velocity:
    get:
      tags: [ScrumAnalytics]
      summary: Get velocity report
      description: |-
        Returns velocity data per sprint, aggregate metrics, and textual interpretation.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
        - name: metricType
          in: query
          schema:
            type: string
            enum: [task_count, story_points, estimation_hours]
            default: task_count
        - name: limit
          in: query
          description: Number of sprints to include (0 = all)
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Velocity report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/VelocityResponse'

  /projects/{project_id}/analytics/burndown:
    get:
      tags: [ScrumAnalytics]
      summary: Get burndown chart data
      description: |-
        Returns daily remaining work vs ideal line for a sprint.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
        - name: metricType
          in: query
          schema:
            type: string
            enum: [task_count, story_points, estimation_hours]
            default: task_count
        - name: sprintId
          in: query
          description: Sprint ID (defaults to active sprint)
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Burndown report
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BurndownResponse'

  # ─── Kanban Analytics ───────────────────────────────────────────────
  /projects/{project_id}/analytics/kanban/cumulative-flow:
    get:
      tags: [KanbanAnalytics]
      summary: Get cumulative flow diagram data
      description: |-
        Returns daily snapshots of task counts per board column for the last 30 days.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: Cumulative flow data
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CumulativeFlowResponse'

  /projects/{project_id}/analytics/kanban/cycle-time-scatter:
    get:
      tags: [KanbanAnalytics]
      summary: Get cycle time scatter plot data
      description: |-
        Returns cycle time (days) for each completed task.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: Cycle time scatter data
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CycleTimeScatterResponse'

  /projects/{project_id}/analytics/kanban/throughput:
    get:
      tags: [KanbanAnalytics]
      summary: Get throughput with trend line
      description: |-
        Returns weekly completed-task counts for the last 8 weeks with a linear regression trend line.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: Throughput and trend data
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ThroughputResponse'

  /projects/{project_id}/analytics/kanban/wip-age:
    get:
      tags: [KanbanAnalytics]
      summary: Get WIP age for in-progress tasks
      description: |-
        Returns tasks currently in in_progress or paused columns with age in days since entering the work column.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: WIP age data
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/WipAgeResponse'

  /projects/{project_id}/analytics/kanban/wip:
    get:
      tags: [KanbanAnalytics]
      summary: Get WIP history
      description: |-
        Returns daily WIP count and limit for the last 30 days.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: WIP history data
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/WipHistoryResponse'

  /projects/{project_id}/analytics/kanban/cycle-time-distribution:
    get:
      tags: [KanbanAnalytics]
      summary: Get cycle time distribution
      description: |-
        Returns histogram of cycle times across completed tasks.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: Cycle time distribution
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/DistributionResponse'

  /projects/{project_id}/analytics/kanban/throughput-distribution:
    get:
      tags: [KanbanAnalytics]
      summary: Get throughput distribution
      description: |-
        Returns histogram of weekly throughput values.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
      responses:
        '200':
          description: Throughput distribution
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/DistributionResponse'

  /projects/{project_id}/analytics/kanban/monte-carlo:
    get:
      tags: [KanbanAnalytics]
      summary: Monte Carlo forecast
      description: |-
        Throughput-based Monte Carlo simulation (10 000 iterations).
        Predicts when a given number of tasks will be completed based on historical weekly throughput.

        Supports optional task filtering — see **Analytics filter parameters** below.
      parameters:
        - $ref: '#/components/parameters/ProjectIdPath'
        - $ref: '#/components/parameters/BoardIdQuery'
        - $ref: '#/components/parameters/AnalyticsFieldFilter'
        - name: task_count
          in: query
          required: true
          description: Number of tasks to forecast completion for
          schema:
            type: integer
            minimum: 1
        - name: weeks
          in: query
          required: false
          description: Number of historical weeks to sample throughput from (default 12, minimum 2)
          schema:
            type: integer
            minimum: 2
            default: 12
        - name: target_date
          in: query
          required: false
          description: Target date to calculate completion probability for (YYYY-MM-DD)
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Monte Carlo forecast
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/MonteCarloResponse'

  # ─── Sprints (standalone) ───────────────────────────────────────────
  /sprints/{sprint_id}:
    get:
      tags: [Sprints]
      summary: Get sprint details
      parameters:
        - $ref: '#/components/parameters/SprintIdPath'
      responses:
        '200':
          description: Sprint details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SprintResponse'
    patch:
      tags: [Sprints]
      summary: Update sprint
      parameters:
        - $ref: '#/components/parameters/SprintIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSprintRequest'
      responses:
        '200':
          description: Updated sprint
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SprintResponse'
    delete:
      tags: [Sprints]
      summary: Delete sprint
      parameters:
        - $ref: '#/components/parameters/SprintIdPath'
      responses:
        '204':
          description: Deleted

  /sprints/{sprint_id}/start:
    post:
      tags: [Sprints]
      summary: Start sprint
      description: |-
        Запускает спринт. Логика при запуске:
        1. **Валидация**: нельзя запустить, если уже есть active-спринт на проекте (400).
        2. **start_date** устанавливается = сегодняшняя дата (игнорируя дату из создания).
        3. **end_date** пересчитывается = start_date + sprint_duration_weeks проекта (fallback 2 недели).
        4. **Каскадный сдвиг**: все последующие planned-спринты с пересекающимися датами сдвигаются вперёд.
        5. Задачам без column_id назначается initial-колонка доски.
      parameters:
        - $ref: '#/components/parameters/SprintIdPath'
      responses:
        '200':
          description: Started sprint
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SprintResponse'
        '400':
          description: |-
            Ошибки валидации:
            - Нельзя запустить завершённый спринт
            - Уже есть активный спринт на проекте

  /sprints/{sprint_id}/tasks:
    get:
      tags: [Sprints]
      summary: Get all tasks for a sprint
      description: Returns full task details for the given sprint, including completed sprints.
      parameters:
        - $ref: '#/components/parameters/SprintIdPath'
      responses:
        '200':
          description: Sprint tasks
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TaskResponse'

  /sprints/{sprint_id}/complete:
    post:
      tags: [Sprints]
      summary: Complete sprint
      description: |-
        Завершает спринт. Незавершённые задачи обрабатываются согласно `incomplete_tasks_action`:
        - `backlog` — задачи возвращаются в product backlog, column_id сбрасывается.
        - `next_sprint` — задачи переносятся в ближайший planned-спринт. Если planned-спринта нет — возвращается 400.
      parameters:
        - $ref: '#/components/parameters/SprintIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteSprintRequest'
      responses:
        '200':
          description: Completed sprint
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SprintResponse'
        '400':
          description: "incomplete_tasks_action=next_sprint, но нет следующего planned-спринта"

  # ─── Boards ─────────────────────────────────────────────────────────
  /boards:
    get:
      tags: [Boards]
      summary: List boards for a project
      parameters:
        - name: project_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Board list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BoardResponse'
    post:
      tags: [Boards]
      summary: Create board
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBoardRequest'
      responses:
        '200':
          description: Created board
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BoardResponse'

  /boards/reorder:
    patch:
      tags: [Boards]
      summary: Reorder boards
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderBoardsRequest'
      responses:
        '204':
          description: Reordered

  /boards/{board_id}:
    get:
      tags: [Boards]
      summary: Get board details
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '200':
          description: Board details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BoardResponse'
    patch:
      tags: [Boards]
      summary: Update board
      description: |
        Синхронизация `tasks.priority` при изменении каталога приоритетов:

        * Если меняется `priority_type` (например, `service_class` → `priority`)
          — в той же транзакции выполняется
          `UPDATE tasks SET priority = NULL WHERE board_id = :board_id`,
          т.е. все ранее выставленные значения обнуляются, потому что они
          принадлежат другому каталогу.
        * Если `priority_type` не меняется, но обновляется `priority_options`
          — обнуляются только те задачи, чьё значение `priority` больше не
          входит в новый список опций. Если `priority_options` становится
          пустым, очищаются все значения приоритетов задач доски.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBoardRequest'
      responses:
        '200':
          description: Updated board
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BoardResponse'
    delete:
      tags: [Boards]
      summary: Delete board
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Columns ────────────────────────────────────────────────────────
  /boards/{board_id}/columns:
    get:
      tags: [Columns]
      summary: List columns
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '200':
          description: Column list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ColumnResponse'
    post:
      tags: [Columns]
      summary: Create column
      description: |-
        Создаёт колонку на указанной позиции `order` (splice-вставка):
        все существующие колонки с `order >=` указанного сдвигаются на +1.

        После вставки проверяется порядок типов колонок: `initial → in_progress → completed`.
        При нарушении — 400 с кодом `INVALID_COLUMN_ORDER`.

        Для колонок с `system_type = completed` установка `wip_limit` запрещена — 400.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateColumnRequest'
      responses:
        '200':
          description: Created column
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ColumnResponse'
        '400':
          description: Invalid column order or invalid systemType
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /boards/{board_id}/columns/reorder:
    patch:
      tags: [Columns]
      summary: Reorder columns
      description: |-
        Изменяет порядок колонок. Заблокированные колонки (например, «Бэклог спринта» в Scrum)
        не могут быть перемещены — вернётся 400 с кодом `COLUMN_LOCKED`.

        После перестановки проверяется порядок типов: `initial → in_progress → completed`.
        При нарушении — 400 с кодом `INVALID_COLUMN_ORDER`, изменения откатываются.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BoardReorderColumnsRequest'
      responses:
        '204':
          description: Reordered
        '400':
          description: Column locked or invalid column order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /boards/{board_id}/columns/{column_id}:
    patch:
      tags: [Columns]
      summary: Update column
      description: |-
        При изменении `system_type` проверяется порядок колонок: `initial → in_progress → completed`.
        Заблокированные колонки нельзя переименовывать или менять `system_type`.
        При нарушении порядка — 400 с кодом `INVALID_COLUMN_ORDER`, изменения откатываются.

        Для колонок с `system_type = completed` установка `wip_limit` запрещена — 400.
        При смене `system_type` на `completed` WIP-лимит автоматически сбрасывается в null.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/ColumnIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateColumnRequest'
      responses:
        '200':
          description: Updated column
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ColumnResponse'
        '400':
          description: Column locked or invalid column order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Columns]
      summary: Delete column
      description: |-
        Удаляет колонку. После удаления порядок оставшихся колонок пересчитывается последовательно (1, 2, 3, ...).

        **Ограничения:**
        - Нельзя удалить заблокированную колонку (`COLUMN_LOCKED`)
        - Нельзя удалить колонку, если в ней есть задачи (`COLUMN_HAS_TASKS`)
        - На доске должна остаться хотя бы одна колонка каждого типа: `initial`, `in_progress`, `completed` (`INVALID_COLUMN_ORDER`)
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/ColumnIdPath'
      responses:
        '204':
          description: Deleted
        '400':
          description: Column locked, has tasks, or would violate minimum type requirement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Swimlanes ──────────────────────────────────────────────────────
  /boards/{board_id}/swimlanes:
    get:
      tags: [Swimlanes]
      summary: List swimlanes
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '200':
          description: Swimlane list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/SwimlaneResponse'
    post:
      tags: [Swimlanes]
      summary: Create swimlane
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSwimlaneRequest'
      responses:
        '200':
          description: Created swimlane
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SwimlaneResponse'

  /boards/{board_id}/swimlanes/reorder:
    patch:
      tags: [Swimlanes]
      summary: Reorder swimlanes
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BoardReorderSwimlanesRequest'
      responses:
        '204':
          description: Reordered

  /boards/{board_id}/swimlanes/{swimlane_id}:
    patch:
      tags: [Swimlanes]
      summary: Update swimlane
      description: |-
        Для Scrum-проектов установка `wip_limit` запрещена — вернётся 400
        с кодом `SCRUM_WIP_NOT_ALLOWED`. Для Kanban — без ограничений.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/SwimlaneIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSwimlaneRequest'
      responses:
        '200':
          description: Updated swimlane
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/SwimlaneResponse'
        '400':
          description: WIP limits not supported for Scrum swimlanes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Swimlanes]
      summary: Delete swimlane
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/SwimlaneIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Notes ──────────────────────────────────────────────────────────
  /boards/{board_id}/notes:
    get:
      tags: [Notes]
      summary: List board notes
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '200':
          description: Note list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/NoteResponse'

  /boards/columns/{column_id}/notes:
    post:
      tags: [Notes]
      summary: Create note for column
      parameters:
        - $ref: '#/components/parameters/ColumnIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNoteRequest'
      responses:
        '200':
          description: Created note
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/NoteResponse'

  /boards/swimlanes/{swimlane_id}/notes:
    post:
      tags: [Notes]
      summary: Create note for swimlane
      parameters:
        - $ref: '#/components/parameters/SwimlaneIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNoteRequest'
      responses:
        '200':
          description: Created note
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/NoteResponse'

  /boards/notes/{note_id}:
    patch:
      tags: [Notes]
      summary: Update note
      parameters:
        - $ref: '#/components/parameters/NoteIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNoteRequest'
      responses:
        '200':
          description: Updated note
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/NoteResponse'
    delete:
      tags: [Notes]
      summary: Delete note
      parameters:
        - $ref: '#/components/parameters/NoteIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Board Fields (system + custom) ────────────────────────────────
  /boards/{board_id}/fields:
    get:
      tags: [BoardFields]
      summary: List all board fields (system + custom)
      description: |
        Returns every field attached to the board, including system fields
        (`is_system: true`) and user-created custom fields.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '200':
          description: Full field list (system + custom)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/BoardCustomFieldResponse'
    post:
      tags: [BoardFields]
      summary: Create custom field
      description: |
        Creates a new custom field (`is_system` is always `false`).
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBoardCustomFieldRequest'
      responses:
        '200':
          description: Created field
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BoardCustomFieldResponse'

  /boards/{board_id}/fields/{field_id}:
    patch:
      tags: [BoardFields]
      summary: Update field
      description: |
        Updates an existing field.

        **System field restrictions (`is_system: true`):**
        - In general, system fields cannot be edited -- attempting to change
          `name`, `description`, or `is_required` returns `400 SYSTEM_FIELD`.
        - **Exception:** the system fields "Приоритизация" and "Оценка трудозатрат"
          allow updating the `options` array via PATCH.
          Example: `{ "options": ["Низкий", "Средний", "Высокий", "Критичный", "Блокер"] }`
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/FieldIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBoardCustomFieldRequest'
      responses:
        '200':
          description: Updated field
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BoardCustomFieldResponse'
    delete:
      tags: [BoardFields]
      summary: Delete field
      description: |
        Deletes a field. System fields (`is_system: true`) cannot be deleted --
        returns `400 SYSTEM_FIELD`.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/FieldIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Tasks ──────────────────────────────────────────────────────────
  /tasks:
    get:
      tags: [Tasks]
      summary: My tasks / project tasks
      description: |-
        Семантика выборки зависит от наличия `project_id`:
          * **Без `project_id`** — персональный список задач текущего пользователя
            (по JWT): где он автор, исполнитель или наблюдатель. Правило действует
            для всех пользователей без исключений — включая администраторов системы
            (системные права не расширяют персональный список).
          * **С `project_id`** — все задачи указанного проекта (без фильтрации по
            участию пользователя) при условии, что у пользователя есть доступ к
            проекту: он участник или обладает системным правом `system.projects.manage`
            на уровне view/full. Если доступа нет — возвращается 403 ACCESS_DENIED.

        Дополнительный `column_id` сужает выборку до одной колонки в рамках выбранного режима.
      parameters:
        - name: project_id
          in: query
          schema:
            type: string
            format: uuid
        - name: column_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Task list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TaskResponse'
    post:
      tags: [Tasks]
      summary: Create task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '200':
          description: Created task
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TaskResponse'

  /tasks/{task_id}:
    get:
      tags: [Tasks]
      summary: Get task details
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Task details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TaskResponse'
    patch:
      tags: [Tasks]
      summary: Update task
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskRequest'
      responses:
        '200':
          description: Updated task
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TaskResponse'
    delete:
      tags: [Tasks]
      summary: Delete task (soft delete)
      description: Soft delete — устанавливает deleted_at. Тело запроса не требуется.
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /tasks/{task_id}/watchers:
    get:
      tags: [Watchers]
      summary: List task watchers
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Watcher list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TaskWatcherResponse'
    post:
      tags: [Watchers]
      summary: Add watcher
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddWatcherRequest'
      responses:
        '200':
          description: Watcher added

  /tasks/{task_id}/watchers/{member_id}:
    delete:
      tags: [Watchers]
      summary: Remove watcher
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
        - name: member_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Removed

  /tasks/{task_id}/dependencies:
    get:
      tags: [Tasks]
      summary: List task dependencies
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Dependency list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TaskDependencyResponse'
    post:
      tags: [Tasks]
      summary: Add dependency
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddDependencyRequest'
      responses:
        '200':
          description: Dependency added
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TaskDependencyResponse'

  /tasks/{task_id}/dependencies/{dependency_id}:
    delete:
      tags: [Tasks]
      summary: Remove dependency
      description: >
        Удаляет связь между задачами. Связи симметричны и хранятся парой строк
        (прямая + обратная), поэтому удаление по `dependency_id` снимает связь
        с обеих сторон — после этого ни GET /tasks/{task_id}/dependencies, ни
        GET /tasks/{other_id}/dependencies не будут содержать парную запись.
        `dependency_id` должен принадлежать задаче `task_id` (иначе 404).
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
        - name: dependency_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Dependency removed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        nullable: true
                        example: null
        '404':
          description: Dependency not found or does not belong to the task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Access denied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'

  /tasks/{task_id}/checklists:
    get:
      tags: [Tasks]
      summary: List task checklists
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Checklist list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ChecklistResponse'
    post:
      tags: [Tasks]
      summary: Create checklist
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChecklistRequest'
      responses:
        '200':
          description: Created checklist
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ChecklistResponse'

  /tasks/checklists/{checklist_id}:
    patch:
      tags: [Tasks]
      summary: Update checklist name
      parameters:
        - name: checklist_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateChecklistRequest'
      responses:
        '200':
          description: Updated checklist
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ChecklistResponse'
    delete:
      tags: [Tasks]
      summary: Delete checklist
      parameters:
        - name: checklist_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /tasks/checklists/{checklist_id}/items:
    post:
      tags: [Tasks]
      summary: Add checklist item
      parameters:
        - name: checklist_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChecklistItemRequest'
      responses:
        '200':
          description: Item added
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ChecklistItemResponse'

  /tasks/checklist-items/{item_id}:
    patch:
      tags: [Tasks]
      summary: Update checklist item content
      parameters:
        - name: item_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateChecklistItemRequest'
      responses:
        '200':
          description: Updated item
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ChecklistItemResponse'
    delete:
      tags: [Tasks]
      summary: Delete checklist item
      parameters:
        - name: item_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  /tasks/checklist-items/{item_id}/status:
    patch:
      tags: [Tasks]
      summary: Set checklist item status
      parameters:
        - name: item_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetChecklistItemStatusRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_Null'

  # ─── Task Field Values ──────────────────────────────────────────────
  /tasks/{task_id}/field-values:
    get:
      tags: [Tasks]
      summary: Get task custom field values
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Field values
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/TaskFieldValueResponse' }

  /tasks/{task_id}/field-values/{field_id}:
    put:
      tags: [Tasks]
      summary: Set task custom field value
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
        - name: field_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetTaskFieldValueRequest'
      responses:
        '200':
          description: Value set

  # ─── Comments ──────────────────────────────────────────────────────
  /tasks/{task_id}/comments:
    get:
      tags: [Comments]
      summary: List task comments
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Comments list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/CommentResponse' }
    post:
      tags: [Comments]
      summary: Create comment
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content: { type: string }
                parent_comment_id: { type: string, format: uuid, nullable: true, description: "ID комментария, на который отвечают" }
      responses:
        '200':
          description: Created comment
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data: { $ref: '#/components/schemas/CommentResponse' }

  /tasks/comments/{comment_id}:
    delete:
      tags: [Comments]
      summary: Delete comment
      parameters:
        - name: comment_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Deleted

  # ─── Attachments ───────────────────────────────────────────────────
  /tasks/{task_id}/attachments:
    get:
      tags: [Attachments]
      summary: List task attachments
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Attachments list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: '#/components/schemas/AttachmentResponse' }
    post:
      tags: [Attachments]
      summary: Upload attachment
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Uploaded attachment
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data: { $ref: '#/components/schemas/AttachmentResponse' }

  /tasks/attachments/{attachment_id}/download:
    get:
      tags: [Attachments]
      summary: Download attachment file
      description: Скачивает файл вложения. Возвращает файл с заголовком Content-Disposition.
      parameters:
        - name: attachment_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: File content
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '404':
          description: Attachment not found

  /tasks/attachments/{attachment_id}:
    delete:
      tags: [Attachments]
      summary: Delete attachment
      parameters:
        - name: attachment_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Deleted

  # ─── Tags ──────────────────────────────────────────────────────────
  /boards/{board_id}/tags:
    get:
      tags: [Tags]
      summary: List all tags on a board
      description: Returns all tags created on the board (for autocomplete suggestions).
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '200':
          description: List of tags
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TagResponse'

  /boards/{board_id}/tasks/{task_id}/tags:
    post:
      tags: [Tags]
      summary: Add tag to task
      description: Creates a new tag on the board (if it doesn't exist) and assigns it to the task.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddTagRequest'
      responses:
        '200':
          description: Tag added
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TagResponse'
    put:
      tags: [Tags]
      summary: Replace all tags on a task
      description: Atomically replaces all tags on the task with the provided list.
      parameters:
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/TaskIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetTaskTagsRequest'
      responses:
        '200':
          description: Tags replaced
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TagResponse'

  /tasks/{task_id}/tags:
    get:
      tags: [Tags]
      summary: List tags of a task
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
      responses:
        '200':
          description: Task tags
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TagResponse'

  /tasks/{task_id}/tags/{tag_id}:
    delete:
      tags: [Tags]
      summary: Remove tag from task
      description: Removes the tag from the task. If the tag is no longer used by any task, it is automatically deleted.
      parameters:
        - $ref: '#/components/parameters/TaskIdPath'
        - name: tag_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Tag removed

  # ─── Admin: System Roles ────────────────────────────────────────────
  /admin/roles:
    get:
      tags: [AdminRoles]
      summary: List system roles
      responses:
        '200':
          description: Role list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/RoleResponse'
    post:
      tags: [AdminRoles]
      summary: Create system role
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRoleRequest'
      responses:
        '200':
          description: Created role
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RoleResponse'

  /admin/roles/{role_id}:
    get:
      tags: [AdminRoles]
      summary: Get system role
      parameters:
        - $ref: '#/components/parameters/RoleIdPath'
      responses:
        '200':
          description: Role details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RoleResponse'
    put:
      tags: [AdminRoles]
      summary: Update system role
      description: |
        Системная роль с is_admin=true полностью неизменяема — нельзя менять имя, описание и права.
      parameters:
        - $ref: '#/components/parameters/RoleIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRoleRequest'
      responses:
        '200':
          description: Updated role
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RoleResponse'
        '403':
          description: System admin role is immutable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                success: false
                data: null
                error: { code: SYSTEM_ADMIN_ROLE, message: "Системная роль администратора неизменяема" }
    delete:
      tags: [AdminRoles]
      summary: Delete system role
      description: |
        Системная роль с is_admin=true не может быть удалена.
        Роль, назначенная хотя бы одному пользователю, не может быть удалена.
      parameters:
        - $ref: '#/components/parameters/RoleIdPath'
      responses:
        '204':
          description: Deleted
        '400':
          description: Role has members assigned (ROLE_HAS_MEMBERS)
        '403':
          description: System admin role cannot be deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                success: false
                data: null
                error: { code: SYSTEM_ADMIN_ROLE, message: "Нельзя удалить системную роль администратора" }

  # ─── Admin: Users ───────────────────────────────────────────────────
  /admin/users:
    get:
      tags: [AdminUsers]
      summary: List all users (admin)
      description: |
        Страница пользователей с серверными фильтрами + карточки статистики.

        В ответе:
        - `users[]` — страница под limit/offset и применённые фильтры.
        - `total` — число записей, подходящих под фильтры (без учёта limit/offset).
        - `active_count` / `inactive_count` — глобальные счётчики по всему множеству
          пользователей (`is_active=true` / `is_active=false`). Не зависят от
          фильтров `q`, `is_active`, `role_id` — чтобы карточки на UI не «плясали»
          при вводе в поиск. `include_deleted` действует на оба счётчика.

        `limit=0` — дешёвый режим «только счётчики»: возвращает пустой `users[]`
        и значения `total`/`active_count`/`inactive_count`.

        **Порядок сортировки** (применяется после фильтров q/is_active/role_id,
        до limit/offset): `ORDER BY LOWER(full_name) ASC, id ASC`.
        Регистро-независимо, кириллица сортируется по-русски корректно; `id` —
        стабильный tiebreaker, чтобы запись с одинаковым ФИО не перескакивала
        между страницами пагинации.
      parameters:
        - name: limit
          in: query
          description: |
            Размер страницы. `0` — только статистика без выгрузки users[].
            Отрицательное/невалидное значение трактуется как 20.
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: includeDeleted
          in: query
          description: Включать soft-deleted пользователей.
          schema:
            type: boolean
            default: false
        - name: q
          in: query
          description: Поиск ILIKE по username, email, full_name, position.
          schema:
            type: string
        - name: is_active
          in: query
          description: Фильтр по статусу (`true` — активные, `false` — деактивированные).
          schema:
            type: boolean
        - name: role_id
          in: query
          description: Только пользователи, которым назначена указанная системная роль.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [users, total, active_count, inactive_count]
                        properties:
                          users:
                            type: array
                            items:
                              $ref: '#/components/schemas/AdminUserResponse'
                          total:
                            type: integer
                            format: int64
                            description: Число записей под применённые фильтры.
                          active_count:
                            type: integer
                            format: int64
                            description: |
                              Глобальный счётчик активных пользователей
                              (`is_active=true`). Не зависит от q/is_active/role_id.
                          inactive_count:
                            type: integer
                            format: int64
                            description: |
                              Глобальный счётчик заблокированных/деактивированных
                              (`is_active=false`). Не зависит от q/is_active/role_id.
    post:
      tags: [AdminUsers]
      summary: Create user (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminCreateUserRequest'
      responses:
        '200':
          description: Created user
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AdminUserResponse'

  /admin/users/{id}:
    get:
      tags: [AdminUsers]
      summary: Get user (admin)
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      responses:
        '200':
          description: User details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AdminUserResponse'
    put:
      tags: [AdminUsers]
      summary: Update user (admin)
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminUpdateUserRequest'
      responses:
        '200':
          description: Updated user
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AdminUserResponse'
    delete:
      tags: [AdminUsers]
      summary: Delete user (admin)
      parameters:
        - $ref: '#/components/parameters/UserIdPath'
      responses:
        '204':
          description: Deleted

  # ─── Admin: Password Policy ─────────────────────────────────────────
  /admin/password-policy:
    get:
      tags: [AdminPasswordPolicy]
      summary: Get password policy (admin)
      responses:
        '200':
          description: Password policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_PasswordPolicy'
    put:
      tags: [AdminPasswordPolicy]
      summary: Update password policy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePasswordPolicyRequest'
      responses:
        '200':
          description: Updated policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse_PasswordPolicy'

  # ─── Admin: Project Templates ───────────────────────────────────────
  /admin/project-templates/references:
    get:
      tags: [AdminTemplates]
      summary: Get template references
      responses:
        '200':
          description: References
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ReferencesResponse'

  /admin/project-templates:
    get:
      tags: [AdminTemplates]
      summary: List project templates
      responses:
        '200':
          description: Template list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ProjectTemplateListResponse'
    post:
      tags: [AdminTemplates]
      summary: Create project template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateRequest'
      responses:
        '200':
          description: Created template
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectTemplateResponse'

  /admin/project-templates/{template_id}:
    get:
      tags: [AdminTemplates]
      summary: Get template details
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      responses:
        '200':
          description: Template details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectTemplateResponse'
    patch:
      tags: [AdminTemplates]
      summary: Update template
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateRequest'
      responses:
        '200':
          description: Updated template
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectTemplateResponse'
    delete:
      tags: [AdminTemplates]
      summary: Delete template
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      responses:
        '204':
          description: Deleted

  /admin/project-templates/{template_id}/boards:
    post:
      tags: [AdminTemplates]
      summary: Create template board
      description: |
        Creates a new board in a template with default columns and all system
        task fields (`is_system: true`).

        System fields are determined by the template's project type (`scrum` /
        `kanban`). The "Приоритизация" field receives default `options` based on
        the provided `priority_type`, and "Оценка трудозатрат" gets a description
        matching the provided `estimation_unit`. The response includes all created
        system fields in `fields`.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateBoardRequest'
      responses:
        '200':
          description: Created board
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardResponse'

  /admin/project-templates/{template_id}/boards/reorder:
    patch:
      tags: [AdminTemplates]
      summary: Reorder template boards
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderRequest'
      responses:
        '204':
          description: Reordered

  /admin/project-templates/{template_id}/boards/{board_id}:
    patch:
      tags: [AdminTemplates]
      summary: Update template board
      description: |
        Partially updates a template board. Only provided fields are changed.

        To clear `swimlane_group_by`, send `{ "swimlane_group_by": null }`.
        Omitting the field leaves the current value unchanged.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateBoardRequest'
      responses:
        '200':
          description: Updated board
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardResponse'
    delete:
      tags: [AdminTemplates]
      summary: Delete template board
      description: |
        Deletes a board from a template.

        Returns `400 DEFAULT_BOARD_DELETE` if the board has `is_default: true`.
        Returns `400 LAST_BOARD_DELETE` if this is the only board in the template.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      responses:
        '204':
          description: Deleted
        '400':
          description: Cannot delete — board is default or last remaining

  /admin/project-templates/{template_id}/boards/{board_id}/columns:
    post:
      tags: [AdminTemplates]
      summary: Create template board column
      description: |
        Creates a new column at the specified position.

        The `order` field works as an insertion index (like `Array.splice`):
        all existing columns with `order >=` the given value are shifted by +1.
        For example, if columns have orders 1, 2, 3, 4 and a new column is created
        with `order: 2`, the result is: new column at 2, old 2→3, 3→4, 4→5.

        Column order validation (`initial → in_progress → completed`) is **not**
        enforced on create — only on reorder and system_type update.

        Для колонок с `system_type = completed` установка `wip_limit` запрещена — 400.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateBoardColumnRequest'
      responses:
        '200':
          description: Created column
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardColumnResponse'

  /admin/project-templates/{template_id}/boards/{board_id}/columns/reorder:
    patch:
      tags: [AdminTemplates]
      summary: Reorder template columns
      description: |-
        Заблокированные колонки не могут быть перемещены (`COLUMN_LOCKED`).
        После перестановки проверяется порядок: `initial → in_progress → completed` (`INVALID_COLUMN_ORDER`).
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderColumnsRequest'
      responses:
        '204':
          description: Reordered
        '400':
          description: Column locked or invalid column order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/project-templates/{template_id}/boards/{board_id}/columns/{column_id}:
    patch:
      tags: [AdminTemplates]
      summary: Update template column
      description: |-
        Заблокированные колонки нельзя редактировать (`COLUMN_LOCKED`).
        При изменении `system_type` проверяется порядок: `initial → in_progress → completed` (`INVALID_COLUMN_ORDER`).

        Для колонок с `system_type = completed` установка `wip_limit` запрещена — 400.
        При смене `system_type` на `completed` WIP-лимит автоматически сбрасывается в null.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/ColumnIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateBoardColumnRequest'
      responses:
        '200':
          description: Updated column
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardColumnResponse'
        '400':
          description: Column locked or invalid column order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [AdminTemplates]
      summary: Delete template column
      description: |-
        Удаляет колонку шаблона. После удаления порядок пересчитывается последовательно (1, 2, 3, ...).

        **Ограничения:**
        - Нельзя удалить заблокированную колонку (`COLUMN_LOCKED`)
        - На доске должна остаться хотя бы одна колонка каждого типа: `initial`, `in_progress`, `completed` (`INVALID_COLUMN_ORDER`)
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/ColumnIdPath'
      responses:
        '204':
          description: Deleted
        '400':
          description: Column locked or would violate minimum type requirement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/project-templates/{template_id}/boards/{board_id}/swimlanes:
    post:
      tags: [AdminTemplates]
      summary: Create template swimlane
      description: |-
        Для Scrum-шаблонов передача `wip_limit` запрещена — вернётся 400
        с кодом `SCRUM_WIP_NOT_ALLOWED`.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateBoardSwimlaneRequest'
      responses:
        '200':
          description: Created swimlane
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardSwimlaneResponse'
        '400':
          description: WIP limits not supported for Scrum template swimlanes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/project-templates/{template_id}/boards/{board_id}/swimlanes/reorder:
    patch:
      tags: [AdminTemplates]
      summary: Reorder template swimlanes
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderSwimlanesRequest'
      responses:
        '204':
          description: Reordered

  /admin/project-templates/{template_id}/boards/{board_id}/swimlanes/{swimlane_id}:
    patch:
      tags: [AdminTemplates]
      summary: Update template swimlane
      description: |-
        Для Scrum-шаблонов установка `wip_limit` запрещена — вернётся 400
        с кодом `SCRUM_WIP_NOT_ALLOWED`.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/SwimlaneIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateBoardSwimlaneRequest'
      responses:
        '200':
          description: Updated swimlane
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardSwimlaneResponse'
        '400':
          description: WIP limits not supported for Scrum template swimlanes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [AdminTemplates]
      summary: Delete template swimlane
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/SwimlaneIdPath'
      responses:
        '204':
          description: Deleted

  /admin/project-templates/{template_id}/boards/{board_id}/fields:
    post:
      tags: [AdminTemplates]
      summary: Create template custom field
      description: |
        Creates a new custom field for a template board.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateBoardCustomFieldRequest'
      responses:
        '200':
          description: Created field
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardCustomFieldResponse'
        '409':
          description: Field with this name already exists (DUPLICATE_NAME)

  /admin/project-templates/{template_id}/boards/{board_id}/fields/{field_id}:
    patch:
      tags: [AdminTemplates]
      summary: Update template custom field
      description: |
        Updates an existing field in a template board.

        **System field restrictions (`is_system: true`):**
        - In general, system fields cannot be edited -- attempting to change
          `name` or `is_required` returns `400 SYSTEM_FIELD`.
        - **Exception:** the system fields "Приоритизация" and "Оценка трудозатрат"
          allow updating the `options` array via PATCH.
          Example: `{ "options": ["Низкий", "Средний", "Высокий", "Критичный", "Блокер"] }`
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/FieldIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateBoardCustomFieldRequest'
      responses:
        '200':
          description: Updated field
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateBoardCustomFieldResponse'
    delete:
      tags: [AdminTemplates]
      summary: Delete template custom field
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/BoardIdPath'
        - $ref: '#/components/parameters/FieldIdPath'
      responses:
        '204':
          description: Deleted

  /admin/project-templates/{template_id}/project-params:
    post:
      tags: [AdminTemplates]
      summary: Create template project param
      description: |
        Creates a new project parameter for a template.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateProjectParamRequest'
      responses:
        '200':
          description: Created param
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateProjectParamResponse'
        '409':
          description: Param with this name already exists (DUPLICATE_NAME)

  /admin/project-templates/{template_id}/project-params/{param_id}:
    patch:
      tags: [AdminTemplates]
      summary: Update template project param
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/ParamIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateProjectParamRequest'
      responses:
        '200':
          description: Updated param
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateProjectParamResponse'
    delete:
      tags: [AdminTemplates]
      summary: Delete template project param
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/ParamIdPath'
      responses:
        '204':
          description: Deleted

  /admin/project-templates/{template_id}/roles:
    post:
      tags: [AdminTemplates]
      summary: Create template role
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTemplateRoleRequest'
      responses:
        '200':
          description: Created role
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateRoleResponse'

  /admin/project-templates/{template_id}/roles/reorder:
    patch:
      tags: [AdminTemplates]
      summary: Reorder template roles
      description: |
        Батч-перестановка ролей шаблона. Принимает массив пар
        `(role_id, order)` — все роли должны принадлежать указанному шаблону.
        После вызова GET `/admin/project-templates/{template_id}` вернёт
        роли в новом порядке.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orders]
              properties:
                orders:
                  type: array
                  items:
                    type: object
                    required: [role_id, order]
                    properties:
                      role_id: { type: string, format: uuid }
                      order:   { type: integer, format: int32 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessEnvelope'

  /admin/project-templates/{template_id}/roles/{role_id}:
    patch:
      tags: [AdminTemplates]
      summary: Update template role
      description: |
        Роль с is_admin=true: можно менять только name и description.
        Попытка изменить permissions вернёт ошибку TEMPLATE_ADMIN_ROLE.

        Каждый `area` в `permissions[]` валидируется против каталога
        `catalog.AllPermissions` (scope=project). Неизвестные или чужого scope
        коды (например, опечатка `project.project.boards`) приводят к
        `400 INVALID_PERMISSION_CODE` со списком нарушивших значений в message
        — чтобы клиент мог поправить точечно, а не ловить тихую потерю доступа.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/RoleIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTemplateRoleRequest'
      responses:
        '200':
          description: Updated role
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/TemplateRoleResponse'
        '400':
          description: Cannot change permissions of template admin role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                success: false
                data: null
                error: { code: TEMPLATE_ADMIN_ROLE, message: "Нельзя изменять права доступа роли администратора проекта в шаблоне" }
    delete:
      tags: [AdminTemplates]
      summary: Delete template role
      description: |
        Роль с is_admin=true не может быть удалена из шаблона.
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
        - $ref: '#/components/parameters/RoleIdPath'
      responses:
        '204':
          description: Deleted
        '400':
          description: Cannot delete template admin role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
              example:
                success: false
                data: null
                error: { code: TEMPLATE_ADMIN_ROLE, message: "Нельзя удалить роль администратора проекта из шаблона" }

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    UserIdPath:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ProjectIdPath:
      name: project_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    MemberIdPath:
      name: member_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    BoardIdPath:
      name: board_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ColumnIdPath:
      name: column_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SwimlaneIdPath:
      name: swimlane_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    NoteIdPath:
      name: note_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    FieldIdPath:
      name: field_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    TaskIdPath:
      name: task_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SprintIdPath:
      name: sprint_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    RoleIdPath:
      name: role_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ParamIdPath:
      name: param_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    TemplateIdPath:
      name: template_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    MeetingIdPath:
      name: meeting_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    BoardIdQuery:
      name: board_id
      in: query
      description: Board ID (defaults to the project's default board)
      required: false
      schema:
        type: string
        format: uuid
    AnalyticsFieldFilter:
      name: filter_{fieldId}
      in: query
      required: false
      description: |-
        **Analytics filter parameters.** Zero or more query parameters of the form
        `filter_{fieldId}=value1,value2` where `fieldId` is a UUID of a board field
        or the virtual key `__tags__`.

        - Comma-separated values are ORed (task matches if its field equals any listed value).
        - Multiple `filter_*` parameters are ANDed.
        - For multiselect / user_list / tags fields the task matches if at least one of its
          stored values overlaps with the filter values.
        - `filter___tags__=bug,feature` — filter by tag names.
        - When combined with `board_id`, tasks are first limited to the board, then filtered.
        - When absent, all tasks of the project are included (default behaviour).

        Example: `?filter_00000000-0000-0000-0001-000000000008=high,medium&filter___tags__=bug`
      schema:
        type: string
      style: form
      explode: true

  responses:
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TooManyRequests:
      description: Rate limited
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    SuccessEnvelope:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data: {}
        error:
          nullable: true

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        data:
          nullable: true
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string

    SuccessResponse_Null:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          nullable: true
        error:
          nullable: true

    SuccessResponse_AuthResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/AuthResponse'

    SuccessResponse_User:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/UserResponse'

    SuccessResponse_Project:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/ProjectResponse'

    SuccessResponse_Meeting:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/MeetingResponse'

    SuccessResponse_MeetingDetails:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/MeetingDetailsResponse'

    SuccessResponse_PasswordPolicy:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/PasswordPolicyResponse'

    # ── Auth ──
    RegisterRequest:
      type: object
      required: [username, email, password, full_name]
      properties:
        username: { type: string }
        email: { type: string, format: email }
        password: { type: string }
        full_name: { type: string }

    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username: { type: string }
        password: { type: string }

    RefreshRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token: { type: string }

    LogoutRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token: { type: string }

    ChangePasswordRequest:
      type: object
      required: [old_password, new_password]
      properties:
        old_password: { type: string }
        new_password: { type: string }

    AuthResponse:
      type: object
      properties:
        access_token: { type: string }
        refresh_token: { type: string }
        user: { $ref: '#/components/schemas/UserResponse' }
        roles:
          type: array
          items: { $ref: '#/components/schemas/RoleResponse' }

    # ── User ──
    UserResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        username: { type: string }
        email: { type: string }
        full_name: { type: string }
        avatar_url: { type: string, nullable: true }
        position: { type: string, nullable: true }
        on_vacation: { type: boolean }
        is_sick: { type: boolean }
        alt_contact_channel: { type: string, nullable: true }
        alt_contact_info: { type: string, nullable: true }

    UpdateUserProfileRequest:
      type: object
      required: [full_name, email]
      properties:
        full_name: { type: string }
        email: { type: string, format: email }
        position: { type: string, nullable: true }
        on_vacation: { type: boolean }
        is_sick: { type: boolean }
        alt_contact_channel: { type: string, nullable: true }
        alt_contact_info: { type: string, nullable: true }

    # ── Permissions / Reference Data ──
    PermissionDefinitionResponse:
      type: object
      properties:
        code: { type: string }
        scope: { type: string, enum: [system, project] }
        name: { type: string }
        description: { type: string }

    # ── System Roles ──
    RolePermissionResponse:
      type: object
      properties:
        code: { type: string }
        access: { type: string, enum: [full, view, none] }

    RolePermissionRequest:
      type: object
      required: [code, access]
      properties:
        code: { type: string }
        access: { type: string, enum: [full, view, none] }

    RoleResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        is_admin: { type: boolean }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionResponse' }

    CreateRoleRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionRequest' }

    UpdateRoleRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionRequest' }

    ProjectMemberRoleRef:
      type: object
      description: |
        Роль участника проекта в ответе API: id и отображаемое имя.
        Передаётся сразу в обоих полях, чтобы фронт мог рендерить имя без
        дополнительного запроса за списком ролей проекта.
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }

    ProjectRoleResponse:
      type: object
      properties:
        project_id: { type: string, format: uuid }
        project_name: { type: string }
        roles:
          type: array
          items: { $ref: '#/components/schemas/ProjectMemberRoleRef' }
        permissions:
          type: array
          items: { type: string }

    # ── Notifications ──
    NotificationResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        type:
          type: string
          enum: [task_assigned, comment_mention, task_status_change_author, task_status_change_assignee, task_status_change_watcher, meeting_invite, meeting_change, meeting_cancel]
        message: { type: string }
        read: { type: boolean }
        created_at: { type: string, format: date-time }
        task_id: { type: string, format: uuid, nullable: true }
        task_key: { type: string, nullable: true, description: "e.g. ECOM-105" }
        meeting_id: { type: string, format: uuid, nullable: true }
        meeting_name: { type: string, nullable: true }
        meeting_start_time: { type: string, format: date-time, nullable: true, description: "Meeting start time (ISO). Present only for meeting_* notifications." }
        participant_status:
          type: string
          nullable: true
          enum: [pending, accepted, declined]
          description: "Current participation status. Present only for meeting_invite notifications."

    NotificationFeedResponse:
      type: object
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/NotificationResponse' }
        unread_count: { type: integer }

    NotificationSettingResponse:
      type: object
      properties:
        event_type: { type: string }
        enabled: { type: boolean }

    UpdateNotificationSettingItem:
      type: object
      required: [event_type]
      properties:
        event_type: { type: string }
        enabled: { type: boolean }

    # ── Meetings ──
    MeetingResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        project_id: { type: string, format: uuid, nullable: true }
        organizer_id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string, nullable: true }
        meeting_type: { type: string }
        location: { type: string, nullable: true }
        status: { type: string, enum: [active, cancelled] }
        start_time: { type: string, format: date-time }
        end_time: { type: string, format: date-time }

    MeetingDetailsResponse:
      allOf:
        - $ref: '#/components/schemas/MeetingResponse'
        - type: object
          properties:
            participants:
              type: array
              items: { $ref: '#/components/schemas/MeetingParticipantResponse' }

    MeetingParticipantResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        meeting_id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        status: { type: string, enum: [pending, accepted, declined] }

    CreateMeetingRequest:
      type: object
      required: [name, location, start_time, end_time]
      properties:
        project_id: { type: string, format: uuid, nullable: true }
        name: { type: string }
        description: { type: string, nullable: true }
        meeting_type: { type: string, nullable: true }
        location: { type: string }
        start_time: { type: string, format: date-time }
        end_time: { type: string, format: date-time }
        participant_ids:
          type: array
          items: { type: string, format: uuid }

    UpdateMeetingRequest:
      type: object
      properties:
        project_id: { type: string, format: uuid, nullable: true }
        name: { type: string }
        description: { type: string, nullable: true }
        meeting_type: { type: string }
        location: { type: string, nullable: true }
        start_time: { type: string, format: date-time }
        end_time: { type: string, format: date-time }

    # ── Projects ──
    ProjectResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        description: { type: string }
        project_type: { type: string, enum: [scrum, kanban] }
        owner_id: { type: string, format: uuid }
        status: { type: string, enum: [active, archived] }
        sprint_duration_weeks: { type: integer, enum: [1, 2, 3, 4], description: Длительность спринта в неделях (только Scrum) }
        incomplete_tasks_action: { type: string, enum: [backlog, next_sprint], default: backlog, description: "Действие с незавершёнными задачами при завершении спринта" }
        created_at: { type: string, format: date-time }
        owner: { $ref: '#/components/schemas/ProjectOwnerResponse' }

    ProjectOwnerResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        full_name: { type: string }
        avatar_url: { type: string, nullable: true }
        email: { type: string }

    CreateProjectRequest:
      type: object
      required: [name, project_type]
      properties:
        name: { type: string }
        description: { type: string }
        project_type: { type: string, enum: [scrum, kanban] }
        owner_id: { type: string, format: uuid, nullable: true }

    UpdateProjectRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [active, archived] }
        owner_id: { type: string, format: uuid }
        sprint_duration_weeks: { type: integer, enum: [1, 2, 3, 4] }
        incomplete_tasks_action: { type: string, enum: [backlog, next_sprint], description: "Действие с незавершёнными задачами при завершении спринта" }

    ProjectReferencesResponse:
      type: object
      properties:
        column_system_types:
          type: array
          items: { $ref: '#/components/schemas/ReferenceColumnType' }
        field_types:
          type: array
          items: { $ref: '#/components/schemas/ReferenceFieldType' }
        estimation_units:
          type: array
          items: { $ref: '#/components/schemas/ReferenceAvailable' }
        priority_type_options:
          type: array
          items: { $ref: '#/components/schemas/ReferencePriorityType' }
        project_statuses:
          type: array
          items: { $ref: '#/components/schemas/ReferenceKeyName' }
        permission_areas:
          type: array
          items: { $ref: '#/components/schemas/ReferencePermissionArea' }
        access_levels:
          type: array
          items: { $ref: '#/components/schemas/ReferenceKeyName' }

    # ── Project Members ──
    ProjectMemberResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        project_id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        roles:
          type: array
          description: |
            Роли участника в проекте с id и именем. Фронт использует имя
            для отображения, id — для ссылок/правок.
          items: { $ref: '#/components/schemas/ProjectMemberRoleRef' }

    AddMemberRequest:
      type: object
      required: [user_id]
      properties:
        user_id: { type: string, format: uuid }
        roles:
          type: array
          items: { type: string }

    UpdateMemberRolesRequest:
      type: object
      required: [roles]
      properties:
        roles:
          type: array
          items: { type: string }

    # ── Project Roles ──
    ProjectRoleDefinitionResponse:
      type: object
      description: |
        Описание роли внутри проекта. Упорядочены по `order` ASC;
        админ-роли изначально сверху.
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        is_admin: { type: boolean }
        order:
          type: integer
          format: int32
          description: Позиция роли в списке (стабильная сортировка).
        permissions:
          type: array
          items: { $ref: '#/components/schemas/ProjectRoleDefPermissionResponse' }

    ProjectRoleDefPermissionResponse:
      type: object
      properties:
        area: { type: string }
        access: { type: string, enum: [full, view, none] }

    CreateProjectRoleRequest:
      type: object
      required: [name, permissions]
      properties:
        name: { type: string }
        description: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionInput' }

    UpdateProjectRoleRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionInput' }

    RolePermissionInput:
      type: object
      required: [area, access]
      properties:
        area: { type: string }
        access: { type: string, enum: [full, view, none] }

    ProjectPermission:
      type: object
      properties:
        area:
          type: string
          enum: [project.boards, project.tasks, project.sprints, project.settings, project.members, project.roles, project.analytics]
        access: { type: string, enum: [full, view, none] }

    # ── Project Params ──
    ProjectParamResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list] }
        is_system: { type: boolean }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }
        value: { type: string, nullable: true }

    CreateProjectParamRequest:
      type: object
      required: [name, field_type]
      description: |-
        Создание параметра проекта. Если передано `value`, оно валидируется по `field_type`:
        - **text** — любая строка
        - **number** — валидное число
        - **datetime** — ISO 8601 (RFC 3339), год в диапазоне 2000–2100
        - **checkbox** — только `"true"` или `"false"`
        - **select** — значение из `options`
        - **multiselect** — значения через запятую, каждое из `options`
        - **user** — UUID существующего пользователя
        - **user_list** — UUID через запятую, каждый — существующий пользователь
      properties:
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list] }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }
        value: { type: string, nullable: true }

    UpdateProjectParamRequest:
      type: object
      description: |-
        Обновление параметра проекта. Значение `value` валидируется по `field_type`:
        - **text** — любая строка
        - **number** — валидное число
        - **datetime** — ISO 8601 (RFC 3339), год в диапазоне 2000–2100
        - **checkbox** — только `"true"` или `"false"`
        - **select** — значение из `options`
        - **multiselect** — значения через запятую, каждое из `options`
        - **user** — UUID существующего пользователя
        - **user_list** — UUID через запятую, каждый — существующий пользователь

        Если параметр обязательный, `value` не может быть null или пустой строкой.
      properties:
        name: { type: string }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }
        value: { type: string, nullable: true }

    # ── Boards ──
    BoardResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        project_id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string, nullable: true }
        is_default: { type: boolean }
        order: { type: integer }
        priority_type: { type: string, enum: [priority, service_class] }
        estimation_unit: { type: string, enum: [story_points, time] }
        swimlane_group_by: { type: string, nullable: true }
        priority_options:
          type: array
          items: { type: string }
          description: Кастомные опции приоритизации для этой доски

    CreateBoardRequest:
      type: object
      required: [project_id, name]
      properties:
        project_id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        order: { type: integer }
        priority_type: { type: string, enum: [priority, service_class] }
        estimation_unit: { type: string, enum: [story_points, time] }
        swimlane_group_by: { type: string, nullable: true }

    UpdateBoardRequest:
      type: object
      description: |-
        При установке `is_default: true` предыдущая доска по умолчанию в этом проекте
        автоматически теряет флаг `is_default`.
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        is_default: { type: boolean }
        order: { type: integer }
        priority_type: { type: string }
        estimation_unit: { type: string }
        swimlane_group_by: { type: string, nullable: true }
        priority_options:
          type: array
          items: { type: string }
          description: Кастомные опции приоритизации

    ReorderBoardsRequest:
      type: object
      required: [project_id, orders]
      properties:
        project_id: { type: string, format: uuid }
        orders:
          type: array
          items:
            type: object
            required: [board_id, order]
            properties:
              board_id: { type: string, format: uuid }
              order: { type: integer }

    # ── Columns ──
    ColumnResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        board_id: { type: string, format: uuid }
        name: { type: string }
        system_type: { type: string, nullable: true, enum: [initial, in_progress, completed] }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }
        is_locked: { type: boolean }

    CreateColumnRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        system_type: { type: string, enum: [initial, in_progress, completed] }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }

    UpdateColumnRequest:
      type: object
      properties:
        name: { type: string }
        system_type: { type: string, nullable: true }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }

    BoardReorderColumnsRequest:
      type: object
      required: [orders]
      properties:
        orders:
          type: array
          items:
            type: object
            required: [column_id, order]
            properties:
              column_id: { type: string, format: uuid }
              order: { type: integer }

    # ── Swimlanes ──
    SwimlaneResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        board_id: { type: string, format: uuid }
        name: { type: string }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }

    CreateSwimlaneRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }

    UpdateSwimlaneRequest:
      type: object
      properties:
        name: { type: string }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }

    BoardReorderSwimlanesRequest:
      type: object
      required: [orders]
      properties:
        orders:
          type: array
          items:
            type: object
            required: [swimlane_id, order]
            properties:
              swimlane_id: { type: string, format: uuid }
              order: { type: integer }

    # ── Notes ──
    NoteResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        column_id: { type: string, format: uuid, nullable: true }
        swimlane_id: { type: string, format: uuid, nullable: true }
        content: { type: string }

    CreateNoteRequest:
      type: object
      required: [content]
      properties:
        content: { type: string }

    UpdateNoteRequest:
      type: object
      properties:
        content: { type: string }

    # ── Custom Fields ──
    BoardCustomFieldResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list, priority, estimation] }
        is_system: { type: boolean }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    CreateBoardCustomFieldRequest:
      type: object
      required: [name, field_type]
      properties:
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list] }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    UpdateBoardCustomFieldRequest:
      type: object
      properties:
        name: { type: string }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    # ── Tasks ──
    TaskResponse:
      type: object
      description: |-
        В контексте задач все поля owner/executor используют member_id (участник проекта), а не user_id.
        Для получения информации о пользователе используйте `owner_user_id` / `executor_user_id`.
        `column_id` может быть null для задач в бэклоге (до запуска спринта).
        При запуске спринта задачи получают column_id первой initial-колонки доски.
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        project_id: { type: string, format: uuid }
        board_id: { type: string, format: uuid }
        owner_member_id: { type: string, format: uuid, description: "ID участника проекта (members.id), назначившего задачу" }
        executor_member_id: { type: string, format: uuid, nullable: true, description: "ID участника проекта (members.id), исполнителя задачи" }
        owner_user_id: { type: string, format: uuid, nullable: true, description: "ID пользователя (users.id) владельца задачи — для GET /users/{id}" }
        executor_user_id: { type: string, format: uuid, nullable: true, description: "ID пользователя (users.id) исполнителя — для GET /users/{id}" }
        name: { type: string }
        description: { type: string, nullable: true }
        deadline: { type: string, format: date-time, nullable: true }
        column_id: { type: string, format: uuid, nullable: true }
        swimlane_id: { type: string, format: uuid, nullable: true }
        priority: { type: string, nullable: true }
        estimation: { type: string, nullable: true }
        progress: { type: integer, nullable: true }
        created_at: { type: string, format: date-time }
        column_name: { type: string, nullable: true, description: Название колонки (LEFT JOIN, null если column_id is null) }
        column_system_type: { type: string, nullable: true, enum: [initial, in_progress, paused, completed, cancelled], description: Системный тип колонки }
        tags:
          type: array
          items: { $ref: '#/components/schemas/TagResponse' }

    CreateTaskRequest:
      type: object
      required: [project_id, owner_member_id, name]
      description: |-
        Для создания задачи нужен либо `board_id`, либо `column_id`, либо оба.
        - `board_id` без `column_id` → задача создаётся в начальной колонке доски.
        - `column_id` без `board_id` → `board_id` определяется автоматически из колонки.
        - Оба → задача сразу на доске в указанной колонке.

        Опциональные вложенные массивы (checklists, tags, watcher_member_ids, field_values, dependencies)
        позволяют создать задачу со всеми подсущностями атомарно в одной транзакции.
        Если массив не передан или пуст — поведение не меняется.
      properties:
        project_id: { type: string, format: uuid }
        board_id: { type: string, format: uuid, description: Доска для задачи }
        owner_member_id: { type: string, format: uuid }
        executor_member_id: { type: string, format: uuid, nullable: true }
        name: { type: string }
        description: { type: string }
        deadline: { type: string, format: date-time, nullable: true }
        column_id: { type: string, format: uuid, nullable: true, description: Опционально — колонка (null для бэклога) }
        swimlane_id: { type: string, format: uuid, nullable: true }
        priority: { type: string, nullable: true }
        estimation: { type: string, nullable: true }
        checklists:
          type: array
          description: Чек-листы, создаваемые вместе с задачей
          items:
            type: object
            required: [name]
            properties:
              name: { type: string }
              items:
                type: array
                items:
                  type: object
                  required: [content]
                  properties:
                    content: { type: string }
                    is_checked: { type: boolean, default: false }
                    order: { type: integer }
        tags:
          type: array
          description: Имена тегов (создаются на доске если не существуют)
          items: { type: string }
        watcher_member_ids:
          type: array
          description: ID участников проекта, добавляемых как наблюдатели
          items: { type: string, format: uuid }
        field_values:
          type: array
          description: Значения кастомных полей доски
          items:
            type: object
            required: [field_id]
            properties:
              field_id: { type: string, format: uuid }
              value_text: { type: string, nullable: true }
              value_number: { type: string, nullable: true }
              value_datetime: { type: string, format: date-time, nullable: true }
        dependencies:
          type: array
          description: Связи с другими задачами
          items:
            type: object
            required: [depends_on_task_id, type]
            properties:
              depends_on_task_id: { type: string, format: uuid }
              type:
                type: string
                enum: [blocks, is_blocked_by, relates_to, parent, subtask]
        add_to_backlog:
          type: boolean
          default: false
          description: Если true — добавить задачу в бэклог продукта проекта после создания

    UpdateTaskRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        deadline: { type: string, format: date-time, nullable: true }
        executor_member_id: { type: string, format: uuid, nullable: true }
        column_id: { type: string, format: uuid }
        swimlane_id: { type: string, format: uuid, nullable: true }
        priority: { type: string, nullable: true }
        estimation: { type: string, nullable: true }

    CommentResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        task_id: { type: string, format: uuid }
        author_id: { type: string, format: uuid }
        content: { type: string }
        parent_comment_id: { type: string, format: uuid, nullable: true, description: "ID родительского комментария (для ответов)" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    AttachmentResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        task_id: { type: string, format: uuid, nullable: true }
        comment_id: { type: string, format: uuid, nullable: true }
        file_name: { type: string }
        file_size: { type: integer }
        content_type: { type: string }
        uploaded_by: { type: string, format: uuid }
        uploaded_at: { type: string, format: date-time }

    TaskFieldValueResponse:
      type: object
      properties:
        field_id: { type: string, format: uuid }
        value_text: { type: string, nullable: true }
        value_number: { type: number, nullable: true }
        value_datetime: { type: string, format: date-time, nullable: true }

    SetTaskFieldValueRequest:
      type: object
      properties:
        value_text: { type: string, nullable: true }
        value_number: { type: number, nullable: true }
        value_datetime: { type: string, format: date-time, nullable: true }

    TaskWatcherResponse:
      type: object
      properties:
        task_id: { type: string, format: uuid }
        member_id: { type: string, format: uuid }

    AddWatcherRequest:
      type: object
      required: [member_id]
      properties:
        member_id: { type: string, format: uuid }

    TaskDependencyResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        task_id: { type: string, format: uuid }
        depends_on_task_id: { type: string, format: uuid }
        type: { type: string, enum: [blocks, is_blocked_by, relates_to, parent, subtask] }

    AddDependencyRequest:
      type: object
      required: [depends_on_task_id, type]
      properties:
        depends_on_task_id: { type: string, format: uuid }
        type: { type: string, enum: [blocks, is_blocked_by, relates_to, parent, subtask] }

    ChecklistResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        task_id: { type: string, format: uuid }
        name: { type: string }
        items:
          type: array
          items: { $ref: '#/components/schemas/ChecklistItemResponse' }

    ChecklistItemResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        checklist_id: { type: string, format: uuid }
        content: { type: string }
        is_checked: { type: boolean }
        order: { type: integer }

    CreateChecklistRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }

    CreateChecklistItemRequest:
      type: object
      required: [content]
      properties:
        content: { type: string }
        order: { type: integer }

    SetChecklistItemStatusRequest:
      type: object
      required: [is_checked]
      properties:
        is_checked: { type: boolean }

    UpdateChecklistRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }

    UpdateChecklistItemRequest:
      type: object
      required: [content]
      properties:
        content: { type: string }

    # ── Sprints ──
    SprintResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        project_id: { type: string, format: uuid }
        name: { type: string }
        goal: { type: string, nullable: true }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        status: { type: string, enum: [planned, active, completed] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    CreateSprintRequest:
      type: object
      required: [name, start_date]
      properties:
        name: { type: string }
        goal: { type: string, nullable: true }
        start_date: { type: string, format: date }
        duration_weeks: { type: integer, enum: [1, 2, 3, 4] }
        duration_days: { type: integer, minimum: 1, maximum: 28 }

    UpdateSprintRequest:
      type: object
      properties:
        name: { type: string }
        goal: { type: string, nullable: true }
        start_date: { type: string, format: date }
        duration_weeks: { type: integer, enum: [1, 2, 3, 4] }
        duration_days: { type: integer, minimum: 1, maximum: 28 }

    CompleteSprintRequest:
      type: object
      required: [incomplete_tasks_action]
      properties:
        incomplete_tasks_action:
          type: string
          enum: [backlog, next_sprint]
          description: What to do with incomplete tasks

    # ── Scrum Analytics ──
    VelocityResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/VelocitySprintData'
        metrics:
          $ref: '#/components/schemas/VelocityMetrics'
        interpretation:
          type: string

    VelocitySprintData:
      type: object
      properties:
        sprint: { type: string }
        sprint_id: { type: string, format: uuid }
        planned: { type: integer }
        completed: { type: integer }

    VelocityMetrics:
      type: object
      properties:
        average_velocity: { type: number }
        velocity_trend: { type: number }
        completion_rate: { type: number }
        average_sprint_scope: { type: number }
        sprint_count: { type: integer }

    BurndownResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/BurndownDayData'
        sprint_name: { type: string }
        interpretation: { type: string }

    BurndownDayData:
      type: object
      properties:
        day: { type: string }
        remaining: { type: number }
        ideal: { type: number }

    # ── Kanban Analytics ──
    CumulativeFlowResponse:
      type: object
      properties:
        data:
          type: array
          description: Daily snapshots with dynamic column keys
          items:
            type: object
            properties:
              date: { type: string }
            additionalProperties:
              type: integer
        interpretation: { type: string }

    CycleTimeScatterResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CycleTimeScatterPoint'
        interpretation: { type: string }

    CycleTimeScatterPoint:
      type: object
      properties:
        task: { type: string, description: Task key (e.g. PROJ-95) }
        time: { type: number, description: Cycle time in days }

    ThroughputResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ThroughputPoint'
        interpretation: { type: string }

    ThroughputPoint:
      type: object
      properties:
        week: { type: string }
        actual: { type: integer }
        trend: { type: number }

    WipAgeResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WipAgePoint'
        interpretation: { type: string }

    WipAgePoint:
      type: object
      properties:
        task_key: { type: string }
        age_days: { type: number }
        column_name: { type: string }

    WipHistoryResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WipHistoryPoint'
        interpretation: { type: string }

    WipHistoryPoint:
      type: object
      properties:
        date: { type: string }
        wip: { type: integer }
        limit:
          type: integer
          nullable: true

    DistributionResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DistributionBucket'
        interpretation: { type: string }

    DistributionBucket:
      type: object
      properties:
        range: { type: string, description: "Range label (e.g. '0-2', '2-4')" }
        count: { type: integer }

    MonteCarloResponse:
      type: object
      properties:
        percentiles:
          type: array
          items:
            $ref: '#/components/schemas/MonteCarloPercentile'
        chart:
          type: array
          items:
            $ref: '#/components/schemas/MonteCarloChartPoint'
        target_date_probability:
          type: integer
          nullable: true
          description: Probability (0–100) of completing all tasks by the target date. Present only when target_date is provided.

    MonteCarloPercentile:
      type: object
      properties:
        percentile: { type: integer, description: "Percentile value (50, 75, 85, 90, 95)" }
        date: { type: string, format: date, description: "Predicted completion date (YYYY-MM-DD)" }

    MonteCarloChartPoint:
      type: object
      properties:
        date: { type: string, description: "Formatted date for X axis (DD.MM)" }
        probability: { type: integer, description: "Cumulative probability 0–100" }

    # ── Admin: Users ──
    AdminUserResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        username: { type: string }
        email: { type: string }
        full_name: { type: string }
        avatar_url: { type: string, nullable: true }
        position: { type: string, nullable: true }
        on_vacation: { type: boolean }
        is_sick: { type: boolean }
        alt_contact_channel: { type: string, nullable: true }
        alt_contact_info: { type: string, nullable: true }
        is_active: { type: boolean }
        roles:
          type: array
          items: { $ref: '#/components/schemas/AdminRoleResponse' }
        created_at: { type: string, format: date-time }

    AdminRoleResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }

    AdminCreateUserRequest:
      type: object
      required: [username, email, full_name, position, password, role_ids]
      properties:
        username: { type: string, minLength: 3, maxLength: 50 }
        email: { type: string, format: email }
        full_name: { type: string }
        position: { type: string, minLength: 1, description: "Должность сотрудника — обязательное поле" }
        password: { type: string, minLength: 6 }
        is_active: { type: boolean }
        role_ids:
          type: array
          minItems: 1
          description: "Идентификаторы системных ролей — минимум одна обязательна"
          items: { type: string, format: uuid }
        on_vacation: { type: boolean }
        is_sick: { type: boolean }
        alt_contact_channel: { type: string, nullable: true }
        alt_contact_info: { type: string, nullable: true }

    AdminUpdateUserRequest:
      type: object
      description: |
        Все поля необязательные (частичное обновление). Однако если клиент
        явно передаёт `position` или `role_ids`, значения должны быть
        непустыми — сбросить должность в `null`/`""` или очистить список
        ролей нельзя (вернётся `400 VALIDATION_ERROR`).
      properties:
        username: { type: string, minLength: 3, maxLength: 50 }
        email: { type: string, format: email }
        full_name: { type: string }
        position:
          type: string
          minLength: 1
          description: "Должность сотрудника. Если передаётся — должна быть непустой."
        is_active: { type: boolean }
        role_ids:
          type: array
          minItems: 1
          description: "Идентификаторы системных ролей. Если передаются — минимум одна."
          items: { type: string, format: uuid }
        on_vacation: { type: boolean }
        is_sick: { type: boolean }
        alt_contact_channel: { type: string, nullable: true }
        alt_contact_info: { type: string, nullable: true }

    # ── Admin: Password Policy ──
    PasswordPolicyResponse:
      type: object
      properties:
        min_length: { type: integer }
        require_digits: { type: boolean }
        require_lowercase: { type: boolean }
        require_uppercase: { type: boolean }
        require_special: { type: boolean }
        notes: { type: string, nullable: true }
        updated_at: { type: string, format: date-time }
        updated_by: { type: string, nullable: true }

    UpdatePasswordPolicyRequest:
      type: object
      properties:
        min_length: { type: integer, minimum: 1, maximum: 100 }
        require_digits: { type: boolean }
        require_lowercase: { type: boolean }
        require_uppercase: { type: boolean }
        require_special: { type: boolean }
        notes: { type: string }

    # ── Admin: Templates ──
    ProjectTemplateListResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        project_type: { type: string, enum: [scrum, kanban] }
        board_count: { type: integer }

    ProjectTemplateResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        project_type: { type: string, enum: [scrum, kanban] }
        boards:
          type: array
          items: { $ref: '#/components/schemas/TemplateBoardResponse' }
        params:
          type: array
          items: { $ref: '#/components/schemas/TemplateProjectParamResponse' }
        roles:
          type: array
          items: { $ref: '#/components/schemas/TemplateRoleResponse' }

    TemplateBoardResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        is_default: { type: boolean }
        order: { type: integer }
        priority_type: { type: string, enum: [priority, service_class] }
        estimation_unit: { type: string, enum: [story_points, time] }
        swimlane_group_by: { type: string, nullable: true }
        priority_options:
          type: array
          items: { type: string }
        columns:
          type: array
          items: { $ref: '#/components/schemas/TemplateBoardColumnResponse' }
        swimlanes:
          type: array
          items: { $ref: '#/components/schemas/TemplateBoardSwimlaneResponse' }
        fields:
          type: array
          items: { $ref: '#/components/schemas/TemplateBoardCustomFieldResponse' }

    TemplateBoardColumnResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        system_type: { type: string, enum: [initial, in_progress, completed] }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }
        is_locked: { type: boolean }
        note: { type: string, nullable: true }

    TemplateBoardSwimlaneResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }
        note: { type: string, nullable: true }

    TemplateBoardCustomFieldResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list, priority, estimation] }
        is_system: { type: boolean }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    TemplateProjectParamResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list] }
        is_system: { type: boolean }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    TemplateRoleResponse:
      type: object
      description: |
        Роль в шаблоне проекта. В массиве ролей шаблона (`roles` на
        ProjectTemplateResponse) упорядочены по `order` ASC; `id` выступает
        вторичным ключом. Админ-роли (`is_admin=true`) в стартовой раскладке
        идут первыми — но после drag-and-drop `order` может быть любым,
        фронт доверяет присланному порядку.
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        is_admin: { type: boolean }
        order:
          type: integer
          format: int32
          description: |
            Позиция роли в списке. Меняется через
            PATCH `/admin/project-templates/{t}/roles/reorder`.
        permissions:
          type: array
          items: { $ref: '#/components/schemas/TemplateRolePermissionResponse' }

    TemplateRolePermissionResponse:
      type: object
      properties:
        area: { type: string }
        access: { type: string, enum: [full, view, none] }

    CreateTemplateRequest:
      type: object
      required: [name, project_type]
      properties:
        name: { type: string }
        description: { type: string }
        project_type: { type: string, enum: [scrum, kanban] }

    UpdateTemplateRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }

    CreateTemplateBoardRequest:
      type: object
      required: [name, priority_type, estimation_unit]
      properties:
        name: { type: string }
        description: { type: string }
        is_default: { type: boolean }
        priority_type: { type: string, enum: [priority, service_class] }
        estimation_unit: { type: string, enum: [story_points, time] }
        swimlane_group_by: { type: string, nullable: true }

    UpdateTemplateBoardRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        is_default: { type: boolean }
        order: { type: integer }
        priority_type: { type: string }
        estimation_unit: { type: string }
        swimlane_group_by: { type: string, nullable: true }
        priority_options:
          type: array
          items: { type: string }
          description: |
            Кастомные опции системного поля «Приоритизация» (например, свой набор
            названий приоритетов для `priority_type=priority` или классов
            обслуживания для `priority_type=service_class`). Если поле передано —
            массив целиком заменяет текущие опции на доске (пустой `[]`
            интерпретируется как «сбросить»). Если передано *вместе* со сменой
            `priority_type` — явно переданные опции в приоритете над дефолтами
            нового типа; если не передано, но `priority_type` поменялся —
            бэкенд подставляет дефолты нового типа автоматически.

            Те же опции видны через системное поле «Приоритизация» в
            `TemplateBoardResponse.fields[]` (поле синтезируется из
            `priority_options` доски, оба представления синхронизированы).

    ReorderRequest:
      type: object
      required: [orders]
      properties:
        orders:
          type: array
          items:
            type: object
            properties:
              board_id: { type: string, format: uuid }
              order: { type: integer }

    CreateTemplateBoardColumnRequest:
      type: object
      required: [name, system_type]
      properties:
        name: { type: string }
        system_type: { type: string, enum: [initial, in_progress, completed] }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }
        note: { type: string, nullable: true }

    UpdateTemplateBoardColumnRequest:
      type: object
      properties:
        name: { type: string }
        system_type: { type: string }
        wip_limit: { type: integer, nullable: true }
        note: { type: string, nullable: true }

    CreateTemplateBoardSwimlaneRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        wip_limit: { type: integer, nullable: true }
        order: { type: integer }

    UpdateTemplateBoardSwimlaneRequest:
      type: object
      properties:
        wip_limit: { type: integer, nullable: true }
        note: { type: string, nullable: true }

    CreateTemplateBoardCustomFieldRequest:
      type: object
      required: [name, field_type]
      properties:
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list] }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    UpdateTemplateBoardCustomFieldRequest:
      type: object
      description: |
        PATCH обычного (не-системного) кастомного поля доски.

        Системные поля «Приоритизация» и «Оценка трудозатрат» (с
        детерминированными UUID из `domain.SystemBoardFieldIDs`) обрабатываются
        отдельно: PATCH на них принимается и отражается на соответствующих
        полях доски (`priority_options` / `estimation_unit`).

        Рекомендуемый путь обновления опций приоритизации —
        PATCH `/admin/project-templates/{id}/boards/{board_id}` с
        `priority_options`. PATCH на системное поле поддерживается для
        обратной совместимости и возвращает актуальное состояние.
      properties:
        name: { type: string }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    CreateTemplateProjectParamRequest:
      type: object
      required: [name, field_type]
      properties:
        name: { type: string }
        field_type: { type: string, enum: [text, number, datetime, select, multiselect, checkbox, user, user_list] }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    UpdateTemplateProjectParamRequest:
      type: object
      properties:
        name: { type: string }
        is_required:
          type: boolean
          default: false
          description: >
            Через этот эндпоинт создаются/редактируются только кастомные
            параметры. Обязательными (`is_required: true`) могут быть только
            системные параметры — они генерируются сервером и в этом запросе
            не адресуются. Передача `true` для кастомного параметра приводит
            к `400 REQUIRED_CUSTOM_FIELD_NOT_ALLOWED`. Рекомендуется либо
            опускать поле, либо передавать `false`.
        options:
          type: array
          items: { type: string }

    ReorderColumnsRequest:
      type: object
      required: [orders]
      properties:
        orders:
          type: array
          items:
            type: object
            required: [column_id, order]
            properties:
              column_id: { type: string, format: uuid }
              order: { type: integer }

    ReorderSwimlanesRequest:
      type: object
      required: [orders]
      properties:
        orders:
          type: array
          items:
            type: object
            required: [swimlane_id, order]
            properties:
              swimlane_id: { type: string, format: uuid }
              order: { type: integer }

    CreateTemplateRoleRequest:
      type: object
      required: [name, permissions]
      properties:
        name: { type: string }
        description: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionInput' }

    UpdateTemplateRoleRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        permissions:
          type: array
          items: { $ref: '#/components/schemas/RolePermissionInput' }

    # ── References ──
    ReferencesResponse:
      type: object
      properties:
        column_system_types:
          type: array
          items: { $ref: '#/components/schemas/ReferenceColumnType' }
        field_types:
          type: array
          items: { $ref: '#/components/schemas/ReferenceFieldType' }
        estimation_units:
          type: array
          items: { $ref: '#/components/schemas/ReferenceAvailable' }
        priority_type_options:
          type: array
          items: { $ref: '#/components/schemas/ReferencePriorityType' }
        project_statuses:
          type: array
          items: { $ref: '#/components/schemas/ReferenceKeyName' }
        permission_areas:
          type: array
          items: { $ref: '#/components/schemas/ReferencePermissionArea' }
        access_levels:
          type: array
          items: { $ref: '#/components/schemas/ReferenceKeyName' }

    ReferenceColumnType:
      type: object
      properties:
        key: { type: string }
        name: { type: string }
        description: { type: string }

    ReferenceKeyName:
      type: object
      properties:
        key: { type: string }
        name: { type: string }

    ReferenceAvailable:
      type: object
      properties:
        key: { type: string }
        name: { type: string }
        available_for:
          type: array
          items: { type: string }

    ReferenceFieldType:
      type: object
      properties:
        key: { type: string }
        name: { type: string }
        available_for:
          type: array
          items: { type: string }
        allowed_scopes:
          type: array
          description: "Допустимые контексты: board_field (поля задач доски), project_param (параметры проекта)"
          items: { type: string, enum: [board_field, project_param] }

    ReferencePriorityType:
      type: object
      properties:
        key: { type: string }
        name: { type: string }
        available_for:
          type: array
          items: { type: string }
        default_values:
          type: array
          items: { type: string }

    ReferencePermissionArea:
      type: object
      properties:
        area: { type: string }
        name: { type: string }
        description: { type: string }
        available_for:
          type: array
          items: { type: string }

    TagResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        board_id: { type: string, format: uuid }
        name: { type: string }

    AddTagRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }

    SetTaskTagsRequest:
      type: object
      required: [tags]
      properties:
        tags:
          type: array
          items: { type: string }
